"use strict"    //we use this to define standard that we are using newer version of JS(treat older code as newer)

// alert(3 + 3)    //cannot use alert in node as this runs alert gives popup in browser

let name = "Vishal" //string data type
let age = 22    //number type
let isMale = true   //boolean
let location = null //null represents nothing

console.log(typeof age) //type of tells the type of data type

/*let's see what's the type of null*/
console.log(typeof(null))   //you can write in this way also